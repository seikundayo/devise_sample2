<h1>購入商品一覧</h1>
<% if user_signed_in? %>
  <% @items.each do |item| %>
  <p>購入商品：<%= item.name %>,値段：<%= item.price %>,<%= item.user.nickname.present? ? "購入者 #{item.user.nickname}" : "会員番号#{item.user.user.id}" %></p>
  <% end %>
<% else %>
  <p>ログインしてください</p>
<% end %>
